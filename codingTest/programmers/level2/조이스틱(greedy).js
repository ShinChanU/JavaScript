const solution = (name) => {
  let answer; // 조작의 최소값
  // 처음은 A만 있음 위(다음 알파벳), 아래(이전알파벳)
  // "A": 65, "Z": 90 => 26
  let init = ["A"];

  return answer;
};

console.log(solution("JEROEN"));
console.log(solution("JAN"));
